#separator:tab
#html:true
#notetype:Basic
#deck:Design Patterns

What are Design Patterns in software engineering?	<div style="text-align:left">Design Patterns are reusable solutions to common problems in software design. Think of them like blueprints — not finished code, but proven templates for solving recurring challenges. They were popularized by the "Gang of Four" (GoF) book in 1994.<br><br>Senior insight: patterns are a communication tool first. Saying "use a Strategy here" is faster than explaining the whole approach. But don't force patterns where simple code works — that's called "pattern fever."</div>
What are the three main categories of GoF Design Patterns?	<div style="text-align:left">1) Creational — how objects are created.<br>2) Structural — how objects are composed/connected.<br>3) Behavioral — how objects communicate and share responsibilities.<br><br>Analogy: Creational = how you build LEGO pieces, Structural = how you snap them together, Behavioral = what the assembled structure does.</div>
What is the difference between a Design Pattern and an Anti-Pattern?	<div style="text-align:left">A Design Pattern solves a recurring problem well. An Anti-Pattern is a common "solution" that actually makes things worse.<br><br>Example anti-pattern: God Object (one class that does everything).<br><br>Senior insight: many anti-patterns start as patterns applied in the wrong context. Singleton is a pattern, but overusing it becomes the "Singletonitis" anti-pattern.</div>
What is the Singleton pattern?	<div style="text-align:left">Singleton ensures a class has only ONE instance and provides a global access point to it.<br><br>Analogy: there's only one president of a country at a time — you don't create a new one every time someone needs to talk to the government.<br><br>Use case: database connection pools, configuration managers, logging services.</div>
What are the dangers of the Singleton pattern?	<div style="text-align:left">1) Hidden dependencies — code secretly depends on global state.<br>2) Hard to test — you can't easily swap it with a mock.<br>3) Thread-safety issues — two threads might create two instances simultaneously.<br>4) Violates Single Responsibility Principle — manages its own lifecycle AND its business logic.<br><br>Senior insight: in modern codebases, prefer Dependency Injection (DI) over Singleton. DI containers can manage single-instance lifetime without the downsides.</div>
How do you implement a thread-safe Singleton in C#?	<div style="text-align:left">Use the <code>Lazy&lt;T&gt;</code> approach: <code>private static readonly Lazy&lt;MyClass&gt; _instance = new Lazy&lt;MyClass&gt;(() => new MyClass());</code> <code>public static MyClass Instance => _instance.Value;</code> This is thread-safe, lazy (created on first access), and clean. Avoid the double-checked locking pattern — it's error-prone and unnecessary in modern C#.</div>
How does Singleton differ in Python vs C# vs JavaScript?	<div style="text-align:left">Python: use a module-level variable (modules are natural singletons) or <code>__new__</code> override.<br>C#: use <code>Lazy&lt;T&gt;</code> or static readonly field.<br>JavaScript: use ES6 module exports (modules are cached after first import, making them natural singletons).<br><br>Senior insight: in Python and JS, the language itself provides singleton-like behavior through modules, so explicit Singleton classes are rarely needed.</div>
What is the Factory Method pattern?	<div style="text-align:left">Factory Method defines an interface for creating objects but lets subclasses decide which class to instantiate.<br><br>Analogy: a restaurant menu (the factory interface) says "we serve burgers" but each branch (subclass) decides if it's a beef burger or veggie burger. It decouples the client from the concrete class being created.</div>
When should you use Factory Method vs. calling 'new' directly?	<div style="text-align:left">Use Factory Method when: 1) you don't know the exact type at compile time, 2) you want subclasses to control creation, 3) creation logic is complex or varies by context. Stick with <code>new</code> when: the type is known, creation is simple, and there's no variation.<br><br>Common mistake: using a factory for simple objects with no variation — that's over-engineering.</div>
What is the Abstract Factory pattern?	<div style="text-align:left">Abstract Factory provides an interface for creating families of related objects without specifying their concrete classes.<br><br>Analogy: IKEA has different furniture "lines" (modern, classic, rustic). Each line is a factory that produces matching chairs, tables, and sofas. You pick a line, and everything comes out coordinated.<br><br>Use case: cross-platform UI toolkits (Windows widgets vs Mac widgets).</div>
What is the difference between Factory Method and Abstract Factory?	<div style="text-align:left">Factory Method creates ONE product through inheritance (subclass overrides a method). Abstract Factory creates FAMILIES of related products through composition (an object with multiple factory methods).<br>Factory Method = one product, uses inheritance.<br>Abstract Factory = multiple related products, uses composition.<br><br>Senior insight: Abstract Factory often uses Factory Methods internally.</div>
What is the Builder pattern?	<div style="text-align:left">Builder separates the construction of a complex object from its representation, allowing the same construction process to create different representations.<br><br>Analogy: building a custom pizza — you add ingredients step by step (dough, sauce, cheese, toppings) and call "bake" at the end. The order and choices vary, but the process is the same.<br><br>Use case: constructing complex objects like HTTP requests, SQL queries, or UI components.</div>
What problem does the Builder pattern solve that constructors can't?	<div style="text-align:left">Constructors with many parameters (telescoping constructors) are unreadable: <code>new Pizza(true, false, true, 3, "large", null)</code>. Builder makes it clear: <code>Pizza.builder().withCheese().withPepperoni().large().build()</code>. It also allows immutable objects to be built step-by-step.<br><br>Senior insight: in C#, consider Builder when you have more than 3-4 constructor parameters. In Python, keyword arguments reduce the need for Builder.</div>
What is the Prototype pattern?	<div style="text-align:left">Prototype creates new objects by cloning an existing instance instead of building from scratch.<br><br>Analogy: photocopying a document — faster than rewriting it.<br><br>Use case: when object creation is expensive (e.g., objects loaded from a database or requiring heavy computation). You clone the prototype and modify only what differs.</div>
What is the difference between shallow copy and deep copy in the Prototype pattern?	<div style="text-align:left">Shallow copy duplicates the object but shares references to nested objects — change a nested object in the copy and it changes in the original too. Deep copy duplicates everything, including nested objects.<br><br>Common mistake: using shallow copy when your object has mutable nested references, causing subtle bugs. In C#: <code>ICloneable</code> is shallow by default. In Python: use <code>copy.deepcopy()</code> for deep copies.</div>
What is the Adapter pattern?	<div style="text-align:left">Adapter converts the interface of a class into another interface that clients expect.<br><br>Analogy: a power plug adapter — your US laptop plug doesn't fit a European socket, so you use an adapter. The laptop and socket don't change, only the adapter sits between them.<br><br>Use case: integrating legacy code or third-party libraries with incompatible interfaces.</div>
What are the two types of Adapter implementations?	<div style="text-align:left">1) Object Adapter — uses composition: the adapter HOLDS an instance of the adaptee.<br>2) Class Adapter — uses inheritance: the adapter EXTENDS the adaptee (requires multiple inheritance, so not possible in C# or Java).<br><br>Senior insight: prefer Object Adapter (composition over inheritance). It's more flexible and works in all languages.</div>
What is the Bridge pattern?	<div style="text-align:left">Bridge separates an abstraction from its implementation so both can vary independently.<br><br>Analogy: a TV remote (abstraction) and the TV (implementation). You can change the remote without changing the TV, and vice versa. Without Bridge, you'd need SonyRemote, SamsungRemote, SonyAdvancedRemote, SamsungAdvancedRemote — a combinatorial explosion.</div>
When should you use Bridge vs Adapter?	<div style="text-align:left">Adapter fixes incompatible interfaces AFTER the code exists (retrofit). Bridge is designed UPFRONT to prevent coupling between abstraction and implementation.<br>Adapter = "make these work together."<br>Bridge = "keep these independent from the start."<br><br>Senior insight: if you find yourself creating many Adapters for the same hierarchy, you probably needed a Bridge from the beginning.</div>
What is the Composite pattern?	<div style="text-align:left">Composite lets you treat individual objects and compositions of objects uniformly through a tree structure.<br><br>Analogy: a file system — a folder can contain files or other folders, but you can "delete" or "move" both the same way.<br><br>Use case: UI component trees, organizational hierarchies, menus with submenus.</div>
What is a common mistake when implementing the Composite pattern?	<div style="text-align:left">Putting operations on the base component that only make sense for composites (like <code>addChild</code> on a leaf node). This violates the Interface Segregation Principle. Some implementations throw exceptions for unsupported operations on leaves, but that's a runtime error instead of a compile-time check.<br><br>Senior insight: decide if you value transparency (uniform interface, less type-safe) or safety (separate interfaces, more type-safe). Most modern codebases prefer safety.</div>
What is the Decorator pattern?	<div style="text-align:left">Decorator dynamically adds behavior to an object without modifying its class. It wraps the original object, delegating calls to it while adding extra logic.<br><br>Analogy: adding toppings to ice cream — the base is still ice cream, each topping adds flavor without changing the original scoop.<br><br>Use case: streams in C# and Java (BufferedStream wraps FileStream), middleware in web frameworks.</div>
What is the difference between Decorator and Inheritance for extending behavior?	<div style="text-align:left">Inheritance is static (fixed at compile time) and creates tightly coupled hierarchies. Decorator is dynamic (applied at runtime) and uses composition. With inheritance you get class explosion: CoffeeWithMilk, CoffeeWithSugar, CoffeeWithMilkAndSugar... With Decorator: <code>new Sugar(new Milk(new Coffee()))</code>.<br><br>Senior insight: Decorator follows the Open/Closed Principle — open for extension, closed for modification.</div>
What is the Facade pattern?	<div style="text-align:left">Facade provides a simplified interface to a complex subsystem.<br><br>Analogy: a hotel concierge — instead of calling the restaurant, spa, and taxi yourself, you tell the concierge what you need and they handle the complexity.<br><br>Use case: wrapping complex library APIs, simplifying multi-step processes like "place an order" (check inventory + charge payment + send confirmation).</div>
What is the difference between Facade and Adapter?	<div style="text-align:left">Facade simplifies a complex interface into an easier one (changes complexity level). Adapter converts one interface to match another (changes interface shape).<br>Facade = "make this simpler."<br>Adapter = "make this compatible."<br>A Facade often wraps multiple objects; an Adapter typically wraps one.</div>
What is the Flyweight pattern?	<div style="text-align:left">Flyweight reduces memory usage by sharing common data across many similar objects. It separates intrinsic state (shared, immutable) from extrinsic state (unique per instance, passed in).<br><br>Analogy: in a word processor, each character on screen doesn't store its own font data. The font data (intrinsic) is shared; the position (extrinsic) is unique.<br><br>Use case: rendering thousands of trees in a game where they share the same texture/mesh.</div>
When is Flyweight worth the added complexity?	<div style="text-align:left">Only when you have a LARGE number of similar objects (thousands+) consuming significant memory. Profile first — don't optimize prematurely. Flyweight adds complexity through the separation of intrinsic/extrinsic state and requires a factory to manage shared instances.<br><br>Common mistake: applying Flyweight to save kilobytes when your app uses gigabytes elsewhere.<br><br>Senior insight: string interning in C# and Java is a built-in Flyweight for string literals.</div>
What is the Proxy pattern?	<div style="text-align:left">Proxy provides a surrogate or placeholder for another object to control access to it.<br><br>Analogy: a security guard at a building entrance — they check credentials before letting you in, but once inside you interact with the building normally.<br><br>Types: Virtual Proxy (lazy loading), Protection Proxy (access control), Remote Proxy (network communication), Caching Proxy.</div>
What is the difference between Proxy and Decorator?	<div style="text-align:left">Both wrap an object, but their intent differs. Proxy controls ACCESS to the object (security, lazy loading, remote access). Decorator adds BEHAVIOR to the object. Proxy often creates or manages the wrapped object's lifecycle. Decorator receives it from outside.<br><br>Senior insight: if you're adding functionality → Decorator. If you're controlling when/how the object is accessed → Proxy.</div>
What is the Strategy pattern?	<div style="text-align:left">Strategy defines a family of algorithms, encapsulates each one, and makes them interchangeable at runtime.<br><br>Analogy: GPS navigation — you choose a strategy (fastest route, shortest route, avoid tolls) and the GPS uses that algorithm without changing its core logic.<br><br>Use case: sorting algorithms, payment processing methods, compression strategies.</div>
Why is Strategy one of the most important patterns for senior architects?	<div style="text-align:left">It's the foundation of the Open/Closed Principle. Instead of giant <code>if/else</code> or <code>switch</code> blocks that grow every time you add a case, you add a new Strategy class. It enables runtime flexibility, testability (mock strategies easily), and clean separation of concerns.<br><br>Common mistake: using Strategy for 2 simple options — an <code>if/else</code> is fine. Strategy shines when you have 4+ interchangeable behaviors or when the behavior changes at runtime.</div>
How do you implement Strategy in Python vs C# vs JavaScript?	<div style="text-align:left">Python: pass functions directly (first-class functions make Strategy trivial) — <code>sort(key=lambda x: x.age)</code>.<br>C#: use interfaces or delegates/<code>Func&lt;T&gt;</code>.<br>JavaScript: pass functions or objects with a common method.<br><br>Senior insight: in languages with first-class functions (Python, JS), you often don't need formal Strategy classes. A function parameter IS a strategy. The pattern is implicit.</div>
What is the Observer pattern?	<div style="text-align:left">Observer defines a one-to-many relationship where when one object (Subject) changes state, all its dependents (Observers) are notified automatically.<br><br>Analogy: a YouTube subscription — when a channel uploads a video, all subscribers get notified.<br><br>Use case: event systems, UI data binding, pub/sub messaging.</div>
What are the pitfalls of the Observer pattern?	<div style="text-align:left">1) Memory leaks — if observers aren't unsubscribed, they can't be garbage collected (lapsed listener problem).<br>2) Unexpected cascades — one change triggers observers that trigger more observers.<br>3) Order dependency — observers may execute in unpredictable order.<br>4) Threading issues in concurrent systems.<br><br>Senior insight: in modern systems, prefer event buses, reactive streams (RxJS, Reactor), or message brokers over raw Observer — they handle these issues better.</div>
What is the Command pattern?	<div style="text-align:left">Command encapsulates a request as an object, letting you parameterize operations, queue them, log them, or support undo.<br><br>Analogy: a restaurant order ticket — the waiter writes down your order (command), passes it to the kitchen (invoker), which executes it. The ticket can be saved, replayed, or canceled.<br><br>Use case: undo/redo, task queues, macro recording, transaction logging.</div>
How does the Command pattern enable undo/redo?	<div style="text-align:left">Each Command object stores the information needed to both execute AND reverse the action. You maintain a history stack. Undo = pop the last command and call its <code>undo()</code> method. Redo = push it back and call <code>execute()</code> again.<br><br>Common mistake: not storing enough state to fully reverse the operation.<br><br>Senior insight: Command + Memento together create robust undo systems. Event Sourcing in distributed systems is essentially Command pattern at scale.</div>
What is the Template Method pattern?	<div style="text-align:left">Template Method defines the skeleton of an algorithm in a base class, letting subclasses override specific steps without changing the overall structure.<br><br>Analogy: a recipe template — "1) prep ingredients, 2) cook, 3) plate." Each chef (subclass) does these steps differently, but the order is fixed.<br><br>Use case: frameworks (e.g., ASP.NET page lifecycle), data processing pipelines.</div>
What is the difference between Template Method and Strategy?	<div style="text-align:left">Template Method uses INHERITANCE — the base class controls the flow and subclasses override hooks. Strategy uses COMPOSITION — the client picks which algorithm object to use.<br>Template Method = "subclass overrides steps."<br>Strategy = "client chooses the whole algorithm."<br><br>Senior insight: prefer Strategy (composition) over Template Method (inheritance) when possible. Template Method creates tight coupling to the base class and is harder to test.</div>
What is the State pattern?	<div style="text-align:left">State allows an object to change its behavior when its internal state changes, appearing to change its class.<br><br>Analogy: a vending machine — it behaves differently when it has no coins (waiting), has coins (ready to dispense), or is out of stock (rejecting input). Each state is a separate class with its own logic.<br><br>Use case: workflow engines, game character states, UI component states.</div>
What is the difference between State pattern and if/else state management?	<div style="text-align:left"><code>if/else</code> scatters state logic throughout the code and grows messy with each new state. State pattern encapsulates each state's behavior in its own class. Adding a new state = adding a new class, no existing code changes.<br><br>Common mistake: using State pattern for simple two-state toggles (just use a boolean).<br><br>Senior insight: State pattern + State Machines are essential for complex workflows in production. Libraries like Stateless (C#) or XState (JS) formalize this.</div>
What is the Chain of Responsibility pattern?	<div style="text-align:left">Chain of Responsibility passes a request along a chain of handlers until one handles it. Each handler decides to process the request OR pass it to the next handler.<br><br>Analogy: customer support escalation — first tier tries, if they can't help, it goes to tier 2, then tier 3.<br><br>Use case: middleware pipelines (Express.js, ASP.NET Core), logging systems, validation chains.</div>
How is Chain of Responsibility used in modern web frameworks?	<div style="text-align:left">Middleware in Express.js and ASP.NET Core IS Chain of Responsibility. Each middleware function processes the request (authentication, logging, CORS, etc.) and calls <code>next()</code> to pass it along. This is one of the most commonly used patterns in production web development.<br><br>Senior insight: the key design decision is whether a handler can STOP the chain (short-circuit) or if every handler always runs. Both designs are valid for different use cases.</div>
What is the Iterator pattern?	<div style="text-align:left">Iterator provides a way to access elements of a collection sequentially without exposing the underlying representation.<br><br>Analogy: a TV remote's "next channel" button — you don't need to know how channels are stored, you just press next.<br><br>Use case: built into virtually every modern language (<code>for...of</code> in JS, <code>foreach</code> in C#, <code>for...in</code> in Python).</div>
Why is Iterator still relevant when languages have built-in iteration?	<div style="text-align:left">You rarely implement Iterator from scratch anymore, but understanding it matters because: 1) Custom iterators for complex data structures (trees, graphs).<br>2) Lazy evaluation — <code>yield</code> in C# and Python generates items on-demand, saving memory.<br>3) Async iterators (<code>IAsyncEnumerable</code> in C#, async generators in JS) are critical for streaming data.<br><br>Senior insight: <code>yield</code>/generators are the modern Iterator pattern and are essential for handling large datasets efficiently.</div>
What is the Mediator pattern?	<div style="text-align:left">Mediator reduces chaotic dependencies between objects by having them communicate through a central mediator instead of directly.<br><br>Analogy: an air traffic control tower — planes don't talk to each other, they talk to the tower which coordinates everything.<br><br>Use case: chat rooms, form validation (fields interact through a mediator), CQRS libraries like MediatR in C#.</div>
What is the difference between Mediator and Observer?	<div style="text-align:left">Observer is a one-to-many broadcast — the subject doesn't know what observers do. Mediator is a hub that actively coordinates interactions between specific objects and contains logic about who communicates with whom.<br>Observer = "I changed, whoever cares can react."<br>Mediator = "I'll coordinate how A and B interact."<br><br>Senior insight: MediatR in C# uses both patterns — it mediates between request handlers while observers (notification handlers) react to events.</div>
What is the Visitor pattern?	<div style="text-align:left">Visitor lets you add new operations to existing object structures without modifying those objects. The objects <code>accept</code> a visitor that performs the operation.<br><br>Analogy: a building inspector — they visit each room (<code>accept</code>) and perform their specific inspection. Adding a fire inspector doesn't require changing the rooms.<br><br>Use case: compilers (AST traversal), document exporters (export to PDF, HTML, JSON).</div>
Why is the Visitor pattern controversial?	<div style="text-align:left">It's powerful but has serious trade-offs: 1) Adding a new element type requires changing ALL visitors (violates Open/Closed for types).<br>2) It breaks encapsulation — visitors access object internals.<br>3) Double dispatch mechanism is confusing for many developers.<br>4) In languages with pattern matching (C# switch expressions, Python <code>match</code>), Visitor is often unnecessary.<br><br>Senior insight: use Visitor when you have a stable set of types but frequently add new operations. If types change often, Visitor is the wrong choice.</div>
What is the Memento pattern?	<div style="text-align:left">Memento captures an object's internal state so it can be restored later, without exposing its internal structure.<br><br>Analogy: saving a game — the save file (memento) captures your progress, and you can restore to that point anytime. The game's internal data structures stay private.<br><br>Use case: undo functionality, checkpointing, transaction rollback.</div>
What is the difference between Memento and Command for undo?	<div style="text-align:left">Command stores the operation and reverses it. Memento stores the entire state snapshot. Command is more memory-efficient (stores just the delta). Memento is simpler but uses more memory (stores full state).<br><br>Senior insight: use Command for fine-grained undo where operations are reversible. Use Memento when operations are too complex to reverse algorithmically — just restore the snapshot. In practice, many systems combine both.</div>
What is the Interpreter pattern?	<div style="text-align:left">Interpreter defines a grammar for a language and an interpreter to process sentences in that language. Each grammar rule becomes a class.<br><br>Analogy: a calculator that understands math expressions — each operator and number is a class that knows how to evaluate itself.<br><br>Use case: SQL parsers, regular expression engines, configuration DSLs, rule engines.</div>
Why is the Interpreter pattern rarely used directly in production?	<div style="text-align:left">It's too slow and inflexible for real-world parsing. Production parsers use parser generators (ANTLR), recursive descent parsers, or parser combinator libraries instead. However, the CONCEPT is everywhere: every DSL, query language, and expression evaluator uses interpreter principles.<br><br>Senior insight: understanding Interpreter helps you design DSLs and config languages, even if you use a parser library instead of implementing the pattern directly.</div>
What are Concurrency Patterns?	<div style="text-align:left">Concurrency Patterns solve problems that arise when multiple threads or processes execute simultaneously. They deal with coordination, synchronization, data sharing, and avoiding issues like deadlocks, race conditions, and starvation. They're critical for senior architects because almost all production systems handle concurrent operations.</div>
What is the Producer-Consumer pattern?	<div style="text-align:left">Producer-Consumer decouples data production from data consumption using a shared buffer (queue). Producers add items; consumers remove and process them independently.<br><br>Analogy: a factory assembly line — workers at station 1 produce parts and place them on a conveyor belt (queue), workers at station 2 pick them up and assemble.<br><br>Use case: message queues, task processing pipelines, log aggregation.</div>
What problems does Producer-Consumer solve?	<div style="text-align:left">1) Speed mismatch — producer and consumer run at different speeds.<br>2) Decoupling — they don't need to know about each other.<br>3) Load buffering — the queue absorbs bursts. Key decisions: bounded vs unbounded queue (bounded prevents memory issues), blocking vs non-blocking (blocking is simpler, non-blocking scales better).<br><br>Common mistake: unbounded queues that grow until the system runs out of memory under load.</div>
What is the Reader-Writer Lock pattern?	<div style="text-align:left">Reader-Writer Lock allows multiple concurrent readers but exclusive access for writers.<br><br>Analogy: a library book — many people can read it simultaneously, but if someone wants to edit it, everyone else must wait.<br><br>Use case: caches, configuration data, shared resources that are read far more often than written. In C#: <code>ReaderWriterLockSlim</code>. In Python: <code>threading.RLock</code> or <code>asyncio</code> locks.</div>
What is the Thread Pool pattern?	<div style="text-align:left">Thread Pool maintains a pool of pre-created worker threads that process incoming tasks from a queue. Instead of creating/destroying threads for each task (expensive), you reuse them.<br><br>Analogy: a taxi stand — taxis wait for passengers instead of building a new taxi for each rider.<br><br>Use case: web servers handling HTTP requests, database connection pools.<br><br>Senior insight: almost all modern runtimes provide built-in pools (C# ThreadPool/.NET Task Scheduler, Java ExecutorService, Python concurrent.futures).</div>
What is the Monitor Object pattern?	<div style="text-align:left">Monitor Object synchronizes method execution to ensure only one thread executes any method on the object at a time. It combines a lock with the object it protects.<br><br>Analogy: a single-occupancy bathroom — only one person at a time, with a lock on the door. In C#: the <code>lock</code> keyword (<code>Monitor.Enter</code>/<code>Monitor.Exit</code>). In Java: <code>synchronized</code> keyword. In Python: <code>threading.Lock</code> in a <code>with</code> statement.<br><br>Common mistake: locking too broadly (entire method) when only a small section needs protection.</div>
What is the Active Object pattern?	<div style="text-align:left">Active Object decouples method invocation from method execution by giving each object its own thread and message queue. Callers enqueue requests and get a <code>Future</code>/<code>Promise</code> back.<br><br>Analogy: sending a letter to a government office — you drop off your request (enqueue), they process it on their schedule, and you check your mailbox for the response (<code>Future</code>).<br><br>Use case: actor systems (Akka, Orleans), UI thread models (WPF Dispatcher).</div>
What is the difference between Active Object and Actor Model?	<div style="text-align:left">They're closely related. Active Object is the GoF-style pattern with explicit <code>Future</code>s and method proxy objects. Actor Model (Erlang, Akka) is a broader concurrency model where actors communicate only through messages — no shared state at all. Actor Model is more restrictive but eliminates shared-state bugs by design.<br><br>Senior insight: Actor Model scales better for distributed systems; Active Object works better within a single process.</div>
What is the Future/Promise pattern?	<div style="text-align:left">Future/Promise represents a value that will be available at some point in the future. It decouples "starting" an operation from "getting the result."<br><br>Analogy: ordering at a fast food counter — you get a receipt number (<code>Future</code>), do other things, and come back when your number is called. In C#: <code>Task&lt;T&gt;</code> / <code>async/await</code>. In JS: <code>Promise</code> / <code>async/await</code>. In Python: <code>asyncio.Future</code> / <code>await</code>.</div>
What are the common pitfalls with Futures/Promises?	<div style="text-align:left">1) Unhandled rejections — forgetting to catch errors on <code>Promise</code>s (JS) leads to silent failures.<br>2) Async deadlocks — in C#, calling <code>.Result</code> or <code>.Wait()</code> on a <code>Task</code> inside a sync context causes deadlock. Use <code>async</code> all the way.<br>3) Fire-and-forget — starting an async operation without <code>await</code>ing it loses errors and ordering guarantees.<br>4) Callback hell / promise chains — use <code>async/await</code> syntax instead.<br><br>Senior insight: "async all the way" is not optional. Mixing sync and async is the #1 source of production deadlocks in C# and Python.</div>
What is the Semaphore pattern?	<div style="text-align:left">Semaphore limits the number of threads that can access a resource simultaneously. Unlike a <code>lock</code> (binary: 0 or 1), a semaphore has a count N.<br><br>Analogy: a parking lot with N spaces — cars enter if spaces are available, wait if full.<br><br>Use case: limiting concurrent database connections, API rate limiting, controlling resource consumption. In C#: <code>SemaphoreSlim</code>. In Python: <code>asyncio.Semaphore</code>.</div>
What is the Double-Checked Locking pattern?	<div style="text-align:left">Double-Checked Locking reduces locking overhead for lazy initialization by checking the condition before AND after acquiring the lock. First check avoids the lock in the common case (already initialized). Second check ensures only one thread initializes.<br><br>Warning: this pattern is broken in many implementations due to memory ordering issues. In C# and Java, you need <code>volatile</code> keyword.<br><br>Senior insight: use language-provided lazy initialization instead (<code>Lazy&lt;T&gt;</code> in C#, enum singleton in Java). Don't hand-roll double-checked locking.</div>
What is the Reactor pattern?	<div style="text-align:left">Reactor handles service requests delivered concurrently to an application by one or more inputs. It demultiplexes events to event handlers synchronously.<br><br>Analogy: a restaurant host who seats guests — they handle one arrival at a time but manage many tables.<br><br>Use case: Node.js event loop, Nginx, Redis — single-threaded servers handling thousands of connections.<br><br>Senior insight: Reactor is why Node.js can handle massive concurrency with one thread. It's the foundation of event-driven architecture.</div>
What is the Proactor pattern and how does it differ from Reactor?	<div style="text-align:left">Reactor handles I/O synchronously (you're notified when data IS ready, then you read it). Proactor handles I/O asynchronously (you initiate the read, the OS notifies you when it's DONE).<br>Analogy: Reactor = "the food is ready, come pick it up."<br>Proactor = "I'll bring the food to your table when it's ready."<br>Proactor is used in Windows IOCP, Boost.Asio.<br><br>Senior insight: Linux traditionally uses Reactor (epoll), Windows uses Proactor (IOCP). io_uring in modern Linux brings Proactor-style to Linux.</div>
Which design patterns are most commonly used in production systems?	<div style="text-align:left">1) Strategy — everywhere (sorting, validation, business rules).<br>2) Observer/Pub-Sub — event systems, reactive UIs, message brokers.<br>3) Factory Method/Abstract Factory — dependency injection, plugin systems.<br>4) Decorator — middleware, streams, logging wrappers.<br>5) Chain of Responsibility — HTTP middleware pipelines.<br>6) Builder — constructing complex configuration/request objects.<br>7) Proxy — lazy loading, caching, API gateways.<br>8) Singleton — (via DI containers) shared services.<br><br>Senior insight: in modern code, many patterns are invisible because frameworks implement them for you. Understanding the patterns helps you USE frameworks correctly.</div>
How does Dependency Injection relate to design patterns?	<div style="text-align:left">DI is the practical evolution of Factory + Strategy + Singleton patterns. Instead of objects creating their own dependencies (<code>new DatabaseService()</code>), dependencies are provided (injected) from outside. This enables testability, loose coupling, and configuration flexibility. DI containers (ASP.NET DI, Spring, Dagger) manage object lifetimes (singleton, scoped, transient).<br><br>Senior insight: DI is the single most impactful architectural pattern for production code maintainability.</div>
What is the Repository pattern and why do architects care about it?	<div style="text-align:left">Repository abstracts data access behind a collection-like interface. The business layer works with repositories (<code>GetUserById</code>, <code>SaveOrder</code>) without knowing if data comes from SQL, NoSQL, an API, or memory. It's a specialized Facade for data access.<br><br>Senior insight: Repository shines when combined with Unit of Work for transactions.<br><br>Common mistake: "generic repositories" (<code>Repository&lt;T&gt;</code>) that leak SQL abstractions or become thin wrappers around ORMs — adding complexity without value.</div>
What is the CQRS pattern?	<div style="text-align:left">Command Query Responsibility Segregation separates read and write operations into different models. Write model (commands) handles business logic and validation. Read model (queries) is optimized for data retrieval.<br><br>Analogy: a restaurant has a kitchen (writes/commands) and a menu board (reads/queries) — they're separate systems optimized for different purposes.<br><br>Senior insight: CQRS shines at scale but is overkill for CRUD apps. Combine with Event Sourcing for full audit trails.</div>
What is the Event Sourcing pattern?	<div style="text-align:left">Instead of storing current state, Event Sourcing stores a sequence of state-changing events. Current state is derived by replaying events.<br><br>Analogy: a bank account — instead of storing "balance = $500," you store every deposit and withdrawal. You can replay to get the balance at any point in time.<br><br>Senior insight: powerful for audit trails and temporal queries, but adds complexity in querying (need projections), versioning events, and handling schema evolution. Not a default choice — use when you genuinely need the full history.</div>
What is the Circuit Breaker pattern?	<div style="text-align:left">Circuit Breaker prevents cascading failures by wrapping calls to external services. States: Closed (normal, requests pass through), Open (failures exceeded threshold, requests fail immediately), Half-Open (testing if service recovered).<br><br>Analogy: an electrical circuit breaker — it trips to prevent damage and resets once safe.<br><br>Use case: microservice communication, external API calls. Libraries: Polly (C#), Resilience4j (Java), opossum (Node.js).</div>
What are the most common mistakes junior developers make with design patterns?	<div style="text-align:left">1) Using patterns where simple code would work ("golden hammer").<br>2) Applying patterns without understanding the problem they solve.<br>3) Implementing patterns exactly as in the GoF book without adapting to modern languages.<br>4) Not recognizing patterns already built into frameworks.<br>5) Over-abstracting small codebases.<br><br>Senior insight: the right time to introduce a pattern is when you feel the PAIN of not having it. If the code is simple and working, don't add a pattern "just in case."</div>
How should a senior architect decide which pattern to use?	<div style="text-align:left">1) Identify the specific problem (not "I need a pattern" but "I need to decouple X from Y").<br>2) Consider the simplest solution first — maybe an <code>if/else</code> is fine.<br>3) Evaluate trade-offs: added complexity vs flexibility gained.<br>4) Consider your team — can they understand and maintain it?<br>5) Consider your language — many patterns are unnecessary in Python/JS that are essential in Java/C#.<br><br>Senior insight: the best architects use patterns invisibly. The code is clean and flexible, and you only notice the pattern if you look for it.</div>
What is the Null Object pattern?	<div style="text-align:left">Null Object replaces null checks with a "do nothing" implementation of an interface. Instead of checking <code>if (logger != null) logger.Log(msg)</code> everywhere, you provide a <code>NullLogger</code> that implements <code>Log()</code> as an empty method.<br><br>Analogy: a <code>/dev/null</code> device — it accepts input and does nothing.<br><br>Use case: default implementations, testing, optional behaviors.<br><br>Senior insight: this eliminates an entire class of NullReferenceExceptions and simplifies code significantly.</div>
What is the Specification pattern?	<div style="text-align:left">Specification encapsulates a business rule into a reusable, combinable object. Each specification has an <code>IsSatisfiedBy(candidate)</code> method. You can combine them with <code>And()</code>, <code>Or()</code>, <code>Not()</code>.<br><br>Analogy: a job listing filter — "5+ years experience AND knows Python OR knows C#".<br><br>Use case: complex business rule validation, query filtering, authorization rules.<br><br>Senior insight: specifications compose well and keep business rules testable and out of repository/service code.</div>
How do patterns differ in microservices vs monolith architectures?	<div style="text-align:left">Monolith: in-process patterns (Strategy, Observer, Factory) dominate. Object references are cheap. Microservices: distributed patterns matter more — Circuit Breaker, Saga, API Gateway (Facade), Event Sourcing, CQRS, Sidecar (Decorator for infrastructure). Communication patterns (<code>sync</code> vs <code>async</code>, request-reply vs event-driven) become critical.<br><br>Senior insight: microservices don't eliminate GoF patterns — they push them to the network level where failures are more likely and latency matters.</div>
What is the Saga pattern for distributed transactions?	<div style="text-align:left">Saga manages transactions spanning multiple microservices by breaking them into a sequence of local transactions, each with a compensating action (rollback).<br><br>Analogy: booking a trip — you book flight, then hotel, then car. If the car booking fails, you cancel the hotel, then cancel the flight (compensating actions in reverse order). Two types: Choreography (events, no coordinator) and Orchestration (central coordinator).<br><br>Senior insight: Orchestration is easier to understand and debug; Choreography scales better but is harder to trace.</div>
What is the Sidecar pattern?	<div style="text-align:left">Sidecar attaches a helper container alongside the main application container, providing supporting features like logging, monitoring, proxying, or configuration.<br><br>Analogy: a motorcycle sidecar — it adds functionality without modifying the motorcycle itself. It's essentially the Decorator pattern for infrastructure.<br><br>Use case: service meshes (Istio/Envoy), log collectors, security proxies.<br><br>Senior insight: Sidecar is the foundation of the service mesh architecture — understanding it is essential for modern cloud-native systems.</div>
What is the Strangler Fig pattern?	<div style="text-align:left">Strangler Fig is a migration pattern for gradually replacing a legacy system with a new one. New features are built in the new system; existing features are migrated incrementally. The old system "strangles" away over time.<br><br>Analogy: like the strangler fig tree that grows around a host tree and eventually replaces it.<br><br>Use case: monolith-to-microservices migration.<br><br>Senior insight: this is the safest way to replace legacy systems. Big-bang rewrites fail over 70% of the time. Strangler Fig lets you deliver value continuously during migration.</div>
What is the difference between Composition and Inheritance when applying design patterns?	<div style="text-align:left">Inheritance = "is-a" relationship, defined at compile time, creates tight coupling. Composition = "has-a" relationship, defined at runtime, creates loose coupling. GoF principle: "Favor composition over inheritance." Most modern patterns use composition (Strategy, Decorator, Adapter via objects).<br><br>Senior insight: inheritance is not evil, but it should be shallow (1-2 levels) and used for genuine type hierarchies, not for code reuse. If you're inheriting for reuse, you probably want composition.</div>
What are the SOLID principles and how do they relate to design patterns?	<div style="text-align:left">S = Single Responsibility (one reason to change). O = Open/Closed (extend behavior without modifying existing code — Strategy, Decorator). L = Liskov Substitution (subtypes must be substitutable — all patterns using polymorphism). I = Interface Segregation (small, focused interfaces — Adapter, Facade). D = Dependency Inversion (depend on abstractions — Factory, Strategy, DI).<br><br>Senior insight: SOLID principles are the WHY; design patterns are the HOW. Learn SOLID first, and patterns become intuitive answers.</div>
